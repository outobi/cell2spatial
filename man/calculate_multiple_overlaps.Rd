% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Overlap_method.R
\name{calculate_multiple_overlaps}
\alias{calculate_multiple_overlaps}
\title{Calculate Multiple Region Signature Overlaps}
\usage{
calculate_multiple_overlaps(
  region_signature_list,
  cell_signature_list,
  universe_size,
  export_dir = NULL
)
}
\arguments{
\item{region_signature_list}{A named list where each element contains a character
vector of gene symbols for a specific region (e.g., "fibroblast_foci", "alveoli").}

\item{cell_signature_list}{A list where each element contains a character vector
of gene symbols for a specific cell type.}

\item{universe_size}{Integer specifying the total number of genes in the universe.
Required parameter, no default value.}

\item{export_dir}{Character string specifying directory path for exporting results.
If NULL (default), no files are exported.}
}
\value{
A named list of data frames, one for each region signature, containing
  overlap analysis results.
}
\description{
Convenience wrapper to calculate overlaps for multiple region signatures
against the same set of cell-type signatures.
}
\examples{
\dontrun{
# Multiple region signatures
region_sigs <- list(
  fibroblast_foci = c("COL1A1", "COL1A2", "ACTA2"),
  alveoli = c("SFTPC", "SFTPA1", "LAMP3"),
  blood_vessel = c("VWF", "PECAM1", "CDH5")
)

# Cell signatures
cell_sigs <- list(
  Fibroblasts = c("COL1A1", "FN1", "DCN"),
  AT2_cells = c("SFTPC", "SFTPA1", "SFTPB"),
  Endothelial = c("VWF", "PECAM1", "CDH5")
)

# Calculate all overlaps
all_results <- calculate_multiple_overlaps(
  region_signature_list = region_sigs,
  cell_signature_list = cell_sigs,
  universe_size,      
  export_dir = "overlap_results"
)
}

}
